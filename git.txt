git clone git@github.com:Pongsatorn/Dow.git
git dir
cd Dow
git checkout -b iss-5555   -----> สร้าง branch 5555
git rebase master  ----->สร้าง branch ในmaster
git add . 
git status 
git add a.txt
git commit -m "add file a.txt" 
git config --global user.email "darw_2537@msn.com"
git config --global user.name "jedsupha"
git checkout master
git merge iss-5555 --no-ff	----->รวมfile ทำในmaster
git push origin master
git log --graph	----wq

dir ---->ดูว่าไฟล์อยู่ที่ไหน
git rm ชื่อfile


